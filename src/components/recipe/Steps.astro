---
import Icon from '../Icon.astro';

interface Props {
  items: string[];
}
const { items } = Astro.props;
---
<h2><Icon field="sections" slug="steps" /> Steps</h2>
<ol>
  {items.map((item) => <li>{item}</li>)}
</ol>
